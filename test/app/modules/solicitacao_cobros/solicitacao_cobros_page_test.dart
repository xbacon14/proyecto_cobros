import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('SolicitacaoCobrosPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SolicitacaoCobrosPage(title: 'SolicitacaoCobros')));
    //  final titleFinder = find.text('SolicitacaoCobros');
    //  expect(titleFinder, findsOneWidget);
  });
}
