import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:proyecto_cobros/app/modules/cobro/cobro_module.dart';

void main() {
  initModule(CobroModule());
  // CobroController cobro;
  //
  setUp(() {
    //     cobro = CobroModule.to.get<CobroController>();
  });

  group('CobroController Test', () {
    //   test("First Test", () {
    //     expect(cobro, isInstanceOf<CobroController>());
    //   });

    //   test("Set Value", () {
    //     expect(cobro.value, equals(0));
    //     cobro.increment();
    //     expect(cobro.value, equals(1));
    //   });
  });
}
